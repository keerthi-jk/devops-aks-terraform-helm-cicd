apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-python.fullname" . }}
  labels:
    {{- include "webapp-python.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-python.selectorLabels" . | nindent 4 }}
